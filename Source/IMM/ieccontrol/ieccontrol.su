SYSTEM IMM

   DEVICE Device1(CPU_TYPE:='x86', RTS_TYPE:='high');

   RESOURCE MASTER_CPU_1 ON Device1; 
   
   TASK TaskSequencer (INTERVAL := T#6ms,
         PRIORITY := 5,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskMid (INTERVAL := T#20ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskSlow (INTERVAL := T#500ms,
         PRIORITY := 13,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskVis (INTERVAL := T#100ms,
         PRIORITY := 14,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;
   
   TASK TaskVisLow (INTERVAL := T#100ms,
         PRIORITY := 16,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;   
   
   TASK TaskHeating (INTERVAL := T#100ms,
         PRIORITY := 8,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskException (INTERVAL := T#0ms,
         PRIORITY := 1,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK PU_Task_3 (INTERVAL := T#0ms,
         PRIORITY := 3,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK PU_Task_7 (INTERVAL := T#0ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK PU_Task_13 (INTERVAL := T#0ms,
         PRIORITY := 13,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK EV_Task_1 (INTERVAL := T#0ms,
         PRIORITY := 1,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK EV_Task_3 (INTERVAL := T#0ms,
         PRIORITY := 3,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK EV_Task_7 (INTERVAL := T#0ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskSim (INTERVAL := T#100ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK Task10ms (INTERVAL := T#10ms,
         PRIORITY := 6,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskAnalog (INTERVAL := T#6ms,
         PRIORITY := 2,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;
		 
   TASK TaskInject (INTERVAL := T#1ms,
         PRIORITY := 2,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;
		 
END_SYSTEM

#END_OF_IEC_PART

@Puma @IecEditor 6 97 @Suu 25
@@@BEG_Comment@@@

@@@END_Comment@@@

@BEG_Contents

@BEG_Export
@TL(109)
SYSTEM IMM

   DEVICE Device1(CPU_TYPE:='x86', RTS_TYPE:='high');

   RESOURCE MASTER_CPU_1 ON Device1; 
   
   TASK TaskSequencer (INTERVAL := T#6ms,
         PRIORITY := 5,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskMid (INTERVAL := T#20ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskSlow (INTERVAL := T#500ms,
         PRIORITY := 13,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskVis (INTERVAL := T#100ms,
         PRIORITY := 14,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;
   
   TASK TaskVisLow (INTERVAL := T#100ms,
         PRIORITY := 16,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;   
   
   TASK TaskHeating (INTERVAL := T#100ms,
         PRIORITY := 8,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskException (INTERVAL := T#0ms,
         PRIORITY := 1,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK PU_Task_3 (INTERVAL := T#0ms,
         PRIORITY := 3,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK PU_Task_7 (INTERVAL := T#0ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK PU_Task_13 (INTERVAL := T#0ms,
         PRIORITY := 13,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK EV_Task_1 (INTERVAL := T#0ms,
         PRIORITY := 1,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK EV_Task_3 (INTERVAL := T#0ms,
         PRIORITY := 3,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK EV_Task_7 (INTERVAL := T#0ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskSim (INTERVAL := T#100ms,
         PRIORITY := 7,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK Task10ms (INTERVAL := T#10ms,
         PRIORITY := 6,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;

   TASK TaskAnalog (INTERVAL := T#6ms,
         PRIORITY := 2,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;
		 
   TASK TaskInject (INTERVAL := T#1ms,
         PRIORITY := 2,
         STACKSIZE := 40960,
         MAXIMUM_ACTIVE_INTERVAL := T#0ms,
         MAXIMUM_ACTIVE_TRIGGER := -1) ON MASTER_CPU_1;
		 
END_SYSTEM
@END_Export

@END_Contents
